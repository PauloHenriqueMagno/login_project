import { BackgroundComponent } from '../../components/atoms';
import { LoginFormComponent } from '../../components/organisms';

const LoginPage = () => {
  return (
    <BackgroundComponent>
      <LoginFormComponent />
    </BackgroundComponent>
  );
}

export default LoginPage;
