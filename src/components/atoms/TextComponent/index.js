import { styled } from "@material-ui/core";

const TextComponent = styled('p')({
  color: '#525357',
  fontSize: '1.15rem',
  fontWeight: '500',
  margin: 'auto',
  width: '100%',
  textAlign: 'center'
});

export default TextComponent;
